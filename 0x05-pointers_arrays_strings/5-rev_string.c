#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void rev_string(char *s)
{
  int i, len;
  s = "My School";
  len = strlen(s);

  for (i = 0; i <= len; i--)
    {
   
